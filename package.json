{
  "name": "tfrc-design-system",
  "version": "1.2.6",
  "private": true,
  "license": "MIT",
  "description": "The Full Remote Company — design system monorepo. Foundation, marketing dialect, product dialect (themed per product). Products consume these as published packages from their own repos — see decisions/0007.md.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/the-full-remote-company/design-system.git"
  },
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "check:contract": "node scripts/check-contract.js",
    "lint:boundaries": "node scripts/lint-boundaries.js",
    "test:verify-consumer": "node scripts/test-verify-consumer.js",
    "verify": "npm run check:contract && npm run lint:boundaries && npm run test:verify-consumer"
  }
}
